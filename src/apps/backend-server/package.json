{
	"name": "@backend-server",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"start": "bun run --hot index.ts",
		"dev": "bun run index.ts",
		"build": "bun build index.ts --outfile dist/index.js"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"@types/express": "^5.0.6"
	},
	"dependencies": {
		"express": "^5.2.1"
	}
}
